/**
 * Time and Date Formatting Utilities
 */

export const formatTimeAgo = (timestamp) => {
    // Placeholder for date-fns or moment logic
    return '2h ago';
};

export const formatDuration = (minutes) => {
    // e.g. 24 -> "24 Minutes", 65 -> "1h 5m"
    return `${minutes} Minutes`;
};
