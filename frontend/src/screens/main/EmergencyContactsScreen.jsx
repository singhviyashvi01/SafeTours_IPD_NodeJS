import React,{useEffect,useState}from'react'; import {View,ScrollView,TouchableOpacity,StyleSheet,Alert}from'react-native'; import {Ionicons}from'@expo/vector-icons'; import {Screen}from'../../components/Screen'; import {Text}from'../../components/Text'; import {DataState}from'../../components/DataState'; import {contactsService}from'../../services/contacts'; import {colors,spacing,shapes}from'../../theme/theme';
export const EmergencyContactsScreen=({navigation})=>{const [items,setItems]=useState(null),[error,setError]=useState('');const load=()=>{setError('');setItems(null);contactsService.list().then(setItems).catch(e=>setError(e.message));};useEffect(()=>{const unsubscribe=navigation.addListener('focus',load);return unsubscribe;},[navigation]);const remove=id=>Alert.alert('Delete contact','Remove this emergency contact?',[{text:'Cancel',style:'cancel'},{text:'Delete',style:'destructive',onPress:()=>contactsService.remove(id).then(load).catch(e=>setError(e.message))}]);return <Screen style={styles.container}><View style={styles.header}><TouchableOpacity onPress={()=>navigation.goBack()}><Ionicons name="arrow-back" size={24} color={colors.primary}/></TouchableOpacity><Text variant="headlineMd" style={styles.title}>Emergency Contacts</Text><TouchableOpacity onPress={()=>navigation.navigate('ContactForm')}><Ionicons name="add" size={26} color={colors.primary}/></TouchableOpacity></View>{!items?<DataState loading={!error} error={error} onRetry={load}/>:items.length===0?<DataState empty emptyText="Add someone you trust for emergencies."/>:<ScrollView contentContainerStyle={styles.content}>{items.map(item=><View style={styles.card} key={item.id}><Ionicons name="person-circle" size={44} color={colors.primary}/><View style={{flex:1}}><Text variant="labelLg" style={{fontWeight:'bold'}}>{item.name}</Text><Text color={colors['on-surface-variant']}>{item.relationship} · {item.phone}</Text></View><TouchableOpacity onPress={()=>navigation.navigate('ContactForm',{contactId:item.id})}><Ionicons name="create-outline" size={20} color={colors.primary}/></TouchableOpacity><TouchableOpacity onPress={()=>remove(item.id)}><Ionicons name="trash-outline" size={20} color={colors.error}/></TouchableOpacity></View>)}</ScrollView>}</Screen>};
const styles=StyleSheet.create({container:{flex:1,backgroundColor:colors.surface},header:{padding:spacing.lg,flexDirection:'row',justifyContent:'space-between',alignItems:'center',borderBottomWidth:1,borderColor:colors['surface-variant']},title:{fontWeight:'bold',color:colors.primary},content:{padding:spacing.lg,gap:spacing.md},card:{flexDirection:'row',alignItems:'center',gap:spacing.sm,padding:spacing.md,backgroundColor:colors['surface-container-low'],borderRadius:shapes.roundedLg,borderWidth:1,borderColor:colors['surface-variant']}});
