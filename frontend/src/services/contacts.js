import { contacts, delay } from './mockStore';
export const contactsService = { list: () => delay([...contacts]), get: id => delay(contacts.find(item => item.id === id)), create: async data => { const item = { ...data, id: `contact-${Date.now()}` }; contacts.push(item); return delay(item); }, update: async (id, data) => { const index = contacts.findIndex(item => item.id === id); if (index < 0) throw new Error('Contact not found'); contacts[index] = { ...contacts[index], ...data }; return delay(contacts[index]); }, remove: async id => { const index = contacts.findIndex(item => item.id === id); if (index < 0) throw new Error('Contact not found'); contacts.splice(index, 1); return delay(); } };
