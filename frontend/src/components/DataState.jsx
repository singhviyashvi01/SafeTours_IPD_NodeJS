import React from 'react'; import { ActivityIndicator, View, StyleSheet } from 'react-native'; import { Text } from './Text'; import { Button } from './Button'; import { colors, spacing } from '../theme/theme';
export const DataState = ({ loading, error, empty, emptyText = 'Nothing to show yet.', onRetry }) => !loading && !error && !empty ? null : <View style={styles.container}>{loading ? <ActivityIndicator size="large" color={colors.primary} /> : <><Text variant="bodyMd" color={colors['on-surface-variant']} style={styles.text}>{error || emptyText}</Text>{error ? <Button title="Try Again" onPress={onRetry} /> : null}</>}</View>;
const styles = StyleSheet.create({ container:{flex:1,alignItems:'center',justifyContent:'center',padding:spacing.xl}, text:{textAlign:'center',marginBottom:spacing.md} });
